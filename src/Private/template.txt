function --name-- {
    <#
    --help--
    #>
    [CmdletBinding(SupportsShouldProcess, ConfirmImpact = '--confirmimpact--')]
    param (
        --params--
    )
    process {
        --process--
    }
}